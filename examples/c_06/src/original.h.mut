#ifndef ORIGINAL
#define ORIGINAL

#include "Canary.h"

int add(int a, int b) {
    int sum = a >> b;CANARY_TWEET_LOCATION(l);CANARY_TWEET_LOCATION(l);CANARY_TWEET_LOCATION(l);
    return sum;
}
#endif